package com.example;

public class Main {
    public static void main(String[] args) throws InterruptedException {

        // Logger.getLogger("org.openqa.selenium").setLevel(Level.OFF);
        // WebDriver driver = new ChromeDriver();

        // driver.manage().window().maximize();
        // driver.get("https://www.tutorialspoint.com/selenium/practice/selenium_automation_practice.php");

        // driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(20));

        // WebElement textBox = driver.findElement(By.id("name"));

        // textBox.sendKeys("1234");

        // WebElement email = driver.findElement(By.name("email"));

        // email.sendKeys("Channelbaby15@gmail.com");

        // WebElement genderSelect =
        // driver.findElement(By.xpath("//label[text()='Male']/preceding-sibling::input"));

        // genderSelect.click();

        // WebElement mobile = driver.findElement(By.xpath("//input[@id='mobile']"));

        // mobile.sendKeys("12345678");

        // WebElement date = driver.findElement(By.xpath("//input[@id='dob']"));

        // date.sendKeys("12/07/2004");

        // WebElement hobbie =
        // driver.findElement(By.xpath("//label[text()='Reading']/preceding-sibling::input"));

        // hobbie.click();

        // WebElement uploadfile =
        // driver.findElement(By.xpath("//input[@id='picture']"));

        // uploadfile.sendKeys("C:\\Users\\admin\\OneDrive\\Hình
        // ảnh\\Screenshots\\Screenshot 2026-08-07 174931.png");

        // WebElement addresss =
        // driver.findElement(By.xpath("//textarea[@id='picture']"));

        // addresss.sendKeys("thanh hóa");

        // Select stateDropdown = new Select(driver.findElement(By.id("state")));

        // stateDropdown.selectByVisibleText("Haryana");

        // Select cityDropdown = new Select(driver.findElement(By.id("city")));

        // cityDropdown.selectByVisibleText("Meerut");

        // WebElement login = driver.findElement(By.xpath("//input[@value='Login']"));

        // login.click();

        // Thread.sleep(5000);

        // driver.quit();
    }
}